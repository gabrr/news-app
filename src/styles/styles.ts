export const setPadding = (top: number, right: number, bottom: number, left: number) => {
    return {
        paddingLeft: left,
        paddingRight: right,
        paddingBottom: bottom,
        paddingTop: top,
    }
}