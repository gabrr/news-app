import { StyleSheet } from 'react-native';
import { Colors } from '.';


export const FONTS = StyleSheet.create({
    header2: {
        fontSize: 20,
        color: Colors.BLACK,
        fontWeight: '700',
        lineHeight: 25,
    },
    body: {
        fontSize: 16,
        color: Colors.BODY,
        fontWeight: '400',
        lineHeight: 25,
    }
})