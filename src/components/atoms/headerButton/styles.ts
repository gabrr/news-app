import { StyleSheet } from 'react-native';
import { Colors } from '../../../styles';

export const styles = StyleSheet.create({
    button: {
        marginRight: 12,
    },
    text: {
        color: Colors.BLUE,
        fontSize: 17,
    }
})